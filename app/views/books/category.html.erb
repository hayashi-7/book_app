<%= render "shared/header" %>

<% @books.each do |book| %>
  <%= book.name %>
   <%= image_tag book.image , class: 'book-img'%>
     
      <%= link_to book_path(book.id), method: :get do %>
   
    
    <%= book.price%>円
    <%= book.author%>
    <%= book.category[:name] %>
     <% end %>
  <%end%>
 
 


